import {Ingredient} from "./Ingredient";
import {DishTypeCategory, MealType, MenuCategory, RecipeCategory} from "./Recipe";

export type Image = {
    id: string,
    name: string,
    url: string,
    size: string

}